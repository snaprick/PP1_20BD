#include <bits/stdc++.h>
using namespace std;
double a,b;
double c;
int count(double a,double b)
{
    cout << setprecision(4)<< sqrt(pow(a,2)+pow(b,2));
}
int main()
{
    cin >> a >> b;
    count(a,b);    
}